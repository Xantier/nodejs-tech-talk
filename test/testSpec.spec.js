'use strict';

var assert = require("assert")

describe('Very much Test', function(){
    it('should assert that 1 is one', function(){
        assert.equal(1, 1, 'One should be equal to one');
    });
});